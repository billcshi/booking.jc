import Link from "next/link"; import { login } from "@/app/actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}) { const {error}=await searchParams; return <main className="center"><form action={login} className="login-card"><Link href="/" className="brand">booking.jc</Link><h1>Host login</h1>{error&&<p className="alert">{error==="rate"?"尝试次数太多，请 15 分钟后再试。":"Credentials 不正确。"}</p>}<label>Username<input name="username" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label><button className="primary">Log in</button></form></main> }
